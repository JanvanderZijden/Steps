#!/usr/bin/python

import pygame
import time
pygame.init()

screen = pygame.display.set_mode((640,480))
image = pygame.image.load("home/pi/usbdrv/Prototype.JPG").c

screen.blit(image, (0,0))

pygame.display.flip()
