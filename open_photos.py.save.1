#!/usr/bin/python

import pygame
import time
pygame.init()

screen = pygame.display.set_mode((1280,960))
image = pygame.image.load("usbdrv/Prototype.JPG")

screen.blit(image, (0,0))
pygame.display.flip()
